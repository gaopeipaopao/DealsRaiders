<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gaope.dealsraiders">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_dr"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Main2Activity" />
        <activity android:name=".View.SearchActivity" />
        <activity android:name=".View.SearchResultActivity" />
        <activity android:name=".View.HomeBannerActivity" />
        <activity android:name=".View.CommentActivity" />
        <activity android:name=".View.StrategyPaintActivity" />
        <activity android:name=".View.MyCollectionActivity" />
        <activity android:name=".View.MyMessageActivity" />
        <activity android:name=".View.HotPushActivity" />
        <activity android:name=".View.HistoryActivity" />
        <activity android:name=".View.SetActivity" />
        <activity android:name=".View.AboutUsActivity" />
        <activity android:name=".View.DengluActivity" />
        <activity android:name=".View.StartActivity"></activity>
    </application>

</manifest>